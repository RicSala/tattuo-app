import { z } from "zod";

// // ###SCHEMAS

// export const MessageSchema = z.number();

// // ###VALIDATORS

// export const validateMessage = (body) => {
//   // validate the message with zod
//   const message = MessageSchema.parse(body.message);
//   return message;
// };
